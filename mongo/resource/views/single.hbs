{{>header}}
{{>nav}}
<section class="container">
    {{#if user}}
 <h1>{{user.id}}</h1>
    <h1>{{user.name}}</h1>
    <h1>{{user.age}}</h1>
    <h1>{{user.email}}</h1>
    {{else}}
    <div class="alert alert-danger">
        no user found
    </div>
    {{/if}}
</section>
   
    {{>footer}}
